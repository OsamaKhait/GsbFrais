{% extends 'base.html.twig' %}

{% block title %}Edit FraisForfait{% endblock %}

{% block body %}
    <div class="max-w-4xl mx-auto p-6 bg-white rounded-lg shadow-md">
        <h1 class="text-2xl font-semibold text-gray-800 mb-6">Edit FraisForfait</h1>

        {{ include('frais_forfait/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="mt-6">
            <a href="{{ path('app_frais_forfait_index') }}" class="inline-block px-6 py-2 bg-gray-500 text-white font-medium rounded-md hover:bg-gray-600 focus:outline-none focus:ring focus:ring-gray-300 transition duration-150">
                Back to list
            </a>
        </div>

        <div class="mt-6">
            <div class="bg-red-100 p-4 rounded-md">
                {{ include('frais_forfait/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
