{% extends 'base.html.twig' %}

{% block title %}Create New Etat{% endblock %}

{% block body %}
    <div class="container mx-auto my-8 p-6 bg-white rounded-lg shadow-lg">
        <h1 class="text-2xl font-bold text-gray-800 mb-6">Create New Etat</h1>

        <div class="mb-6">
            {{ include('etat/_form.html.twig', {'button_label': 'Create'}) }}
        </div>

        <div class="mt-6">
            <a
                    href="{{ path('app_etat_index') }}"
                    class="inline-block text-blue-500 hover:text-blue-700 transition font-medium"
            >
                &larr; Back to list
            </a>
        </div>
    </div>
{% endblock %}
