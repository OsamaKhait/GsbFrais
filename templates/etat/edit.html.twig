{% extends 'base.html.twig' %}

{% block title %}Edit Etat{% endblock %}

{% block body %}
    <div class="container mx-auto my-8 p-6 bg-white rounded-lg shadow-lg">
        <h1 class="text-2xl font-bold text-gray-800 mb-6">Edit Etat</h1>

        <div class="mb-6">
            {{ include('etat/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="flex justify-between items-center mt-6">
            <a
                    href="{{ path('app_etat_index') }}"
                    class="text-blue-600 hover:text-blue-800 font-medium transition duration-150"
            >
                Back to list
            </a>
            {{ include('etat/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
